function isPalindrome(num) {
    if(!num || typeof num !== 'number') {
        return false;
    }
    let length = Math.log(num) / Math.log(10) + 1;
    let frontNum = num;
    let backNum = num;
    let i = length;
    while(i!=0) {

    }
}

console.log(isPalindrome());