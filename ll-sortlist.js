// sort a ll with duplicates
let linkedList = require('./linked-list');

const sortlist = (ll) => {

}

const ll = new linkedList();
const addThese = [4,3,7,8,6];
addThese.forEach(el => ll.insertNode(el));
let sortedList = sortlist(ll);
ll.printAllValues.bind(sortedList);